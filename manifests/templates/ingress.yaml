---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: boottttttttttttt                  # Ingress name
  labels:
    app: boottttttttttttt
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "boottttttttttttt.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: boottttttttttttt               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
